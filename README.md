# AI-Powered Store MVP

A product catalog with an AI recommendation endpoint grounded in products stored in MongoDB.

## Run locally

Configure `server/.env`, start the server and client, then click **Load demo products**.

## Portfolio improvements

Add authentication, cart/checkout, Stripe test payments, product embeddings, behavior tracking, and an admin catalog.
